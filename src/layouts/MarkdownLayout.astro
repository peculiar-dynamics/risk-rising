---
import BaseLayout from "~/layouts/BaseLayout.astro"
import Heading from "~/components/common/Heading.astro";

const { frontmatter } = Astro.props;

---

<BaseLayout title={frontmatter.title} type="article">
  <!-- <h1 class="my-16 w-full text-center text-4xl font-bold">
    <Heading id={frontmatter.link} text={frontmatter.title} />
  </h1>

  <div class="policy space-content container max-w-[80ch]">
    <slot />
  </div> -->
  <article
  class:list={[" prose mt-32 max-w-screen-xl flex flex-col items-center justify-center p-4 dark:prose-invert sm:py-8 sm:px-6 md:px-8", frontmatter.type]}
>
  <h1 class="w-fit text-center text-4xl font-bold">
    <Heading id={frontmatter.link} text={frontmatter.title}/>
  </h1>

  <slot />

</article>
</BaseLayout>

<style lang="css" is:global>

  article :not(img, a, span){
    @apply max-w-[80ch] w-full;
  }
  /* article p, article li {
    @apply max-w-[80ch]
  } */
</style>


  